{
  "name": "snippet-library",
  "version": "1.0.0",
  "description": "Front-end libraries for CSS JS HTML",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify:css": "cleancss -o dist/css/styles.min.css src/css/styles.css",
    "minify:js": "terser src/js/scripts.js -o dist/js/scripts.min.js -c -m",
    "build": "npm run minify:css && npm run minify:js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.42.0"
  }
}