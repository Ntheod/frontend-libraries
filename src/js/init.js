function onLoad() { //initial function here we handle any calls, want to be responsive, hide elements etc etc.
    console.log("We loaded successfully")
    chooseTransition("fade", "in", "main-container", false);
}